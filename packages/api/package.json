{
  "name": "@scanfactory/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute DB --local --file=src/db/schema.sql",
    "db:seed": "wrangler d1 execute DB --local --file=src/db/seed.sql",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.0",
    "vitest": "^1.3.0",
    "wrangler": "^3.28.0"
  }
}
